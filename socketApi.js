// socketApi.js - updated for StateManager + client compatibility
module.exports = function(stateManager) { 
    const s = stateManager; // voor backward compatibility
}
// ... rest of your original socketApi with fixes for set/get ...